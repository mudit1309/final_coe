# ------- Server (Node.js/Express) -------
# Port the API server listens on (default 3001). Client dev server (5173)
# proxies /api/* to this.
PORT=3001

# Admin panel password
ADMIN_PASSWORD=vizshila@123

# Secret used to sign the admin session cookie
ADMIN_COOKIE_SECRET=change-me-in-production

#mongodb database
MONGODB_URI=mongodb://localhost:27017/vizlogic

# 2FA TOKEN (UIID)
TWO_FACTOR_TOKEN=39786f93-7bd6-11ef-8b17-0200cd936042


# Optional: path for the JSON data file (defaults to ./server/data/vizlogic.json)
# DATABASE_PATH=./server/data/vizlogic.json
